
// No clue if I should be using a different port, let's give it a try
// I think we should be using the same port and checking the path when the connection happens

// Should rename both files to reflect that it's the same server - perhaps refactor into a third
// file that calls these